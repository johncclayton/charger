version: '2'
services:
  runtime:
    image: johncclayton/syncthing-cli
    build:
      context: .

    container_name: "syncthing-cli"

    restart: unless-stopped

    ports:
      - "8384:8384"

    networks:
      - net

networks:
  net:
  

